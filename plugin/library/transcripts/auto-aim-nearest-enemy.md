# Auto-Aim Nearest-Enemy Turret (Spaghetti Syntax) — transcript digest

**Source** — `godot-auto-aim-neareast-enemy.md` (raw now in `transcripts/archive/godot-auto-aim-neareast-enemy.md`). 2D top-down horde-survivor auto-aim weapon tutorial.
**Why harvested** — turret rework IN FLIGHT: rotate-to-aim nearest enemy + fire-when-aligned (`entities/weapon/turret_controller.gd`). Validate the build / harvest any better nearest-selection, LOS, tracking, or prediction technique.

**Points**

| #   | Point (technique/claim)                                                                                                                                      | Valid for our stack?                                                                                      | Already learned? | Where / gap                                                                                                                                                                                                                                 | Verdict |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 1   | Candidate gathering: a detector volume (Area2D `get_overlapping_bodies()`) returns all enemies in range, masked to enemy layer only                          | holds with caveat (2D→3D: we use a group query, not an overlap volume — same intent, range gated in code) | covered          | `target_acquisition.gd` `acquire()` = `get_nodes_in_group(target_group="enemies")` + `dist_sq > max_range_sq` cull                                                                                                                          | drop    |
| 2   | Nearest selection: loop candidates, track `shortest = INF`, keep the min-distance winner                                                                     | holds                                                                                                     | covered          | `target_acquisition.gd` L96-99 `SelectionRule.NEAREST`, `best_dist_sq = INF` — identical algorithm                                                                                                                                          | drop    |
| 3   | Re-scan on a timer (0.3s) not every frame; bail early when zero candidates                                                                                   | holds                                                                                                     | covered          | `turret_controller.gd` `reacquire_interval` + `acquire()` `candidates.is_empty(): return null`                                                                                                                                              | drop    |
| 4   | Fire: instantiate projectile, set pos to muzzle, `direction_to(target)` + `look_at`, add to scene root (top_level, not child of weapon) so it lives in world | holds                                                                                                     | covered          | `shooter_attack.gd` `_fire_at_player()` (`Basis.looking_at`, `scene_root.add_child`, `top_level=true`); turret routes through `Gun.try_fire()`                                                                                              | drop    |
| 5   | Frame-independent travel (`dir * speed * delta`), despawn past max range                                                                                     | holds                                                                                                     | covered          | skill `godot-travelling-projectile-3d` (spawn→move→despawn, range cull)                                                                                                                                                                     | drop    |
| 6   | Duck-typed damage seam: `if body.has_method("take_damage")` before applying                                                                                  | holds                                                                                                     | covered          | skill `godot-fps-enemy-combat` `on_hit()` seam + `cast-system` apply chain                                                                                                                                                                  | drop    |
| 7   | Distance/angle WEIGHTING, line-of-sight gate, smooth rotate-to-aim TRACKING, prediction/lead                                                                 | n/a — transcript teaches NONE of these (instant fire at winner, no arc, no LOS, no rotation)              | n/a              | our stack already EXCEEDS: `acquire()` has arc (`arc_half_angle_deg`) + LOS (`PhysicsRayQueryParameters3D mask=1`) + reserved `MOST_CENTRED` angle rule; `turret_controller` does slerp rotate-to-aim + `alignment_tolerance_deg` fire gate | drop    |

**Mapping** — 6/6 taught techniques COVERED; 0 partial; 0 gap; 0 conflicts. Transcript is a strict SUBSET of our turret. Overlap w/ archived `godot-aim-assist.md` (controller aim-assist slowdown tween) — different problem (player-aim ease vs turret target-select); no duplication.

**New technique to adopt now?** NO. Our `acquire()` + slerp rotate-to-aim + fire-when-aligned already does everything the video does and more (3D, arc, LOS, smooth tracking). No better weighting, no prediction/lead in the source to harvest.

**Recommended next** — nothing to act on now. In-flight turret rework (`turret_controller.gd`) is VALIDATED by this source; proceed as built. No agent dispatch.

**Later**

- Lead/prediction (aim where target WILL be) — NOT in this video, NOT in our stack; only matters vs fast crossing targets at long range. Park until turret feels laggy in playtest → then game-designer (is lead wanted) before skill-researcher.
- 2nd `acquire()` consumer (enemy-shooter migration) is already named in `target_acquisition.gd` header — when that lands, the `TargetAcquisition`/auto-aim pattern has 2 consumers and is a real skill-researcher promotion candidate. Not now (1 consumer).
