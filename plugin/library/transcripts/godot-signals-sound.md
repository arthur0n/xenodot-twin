# Godot Signals pt.2 (filename "sound", actually SSOT) — transcript digest

**Source** — `godot-signals-sound.md` (raw now in `transcripts/archive/godot-signals-sound.md`). Drago / "Draku Games", Godot signals pt.2 — same video as the sibling digest `signals-system-basic-drago.md` (dynamic spawns, signal bus, disconnect, SSOT). 2D coin-collect example. **Filename says "sound" but transcript carries NO audio/SFX content** — it is the SSOT/signal-rules video, mislabeled in the drop zone.
**Why harvested** — about to build signals work in this FPS project.

**Dedup note** — content is the SAME video already harvested as `signals-system-basic-drago.md`. This digest is DIFFERENTIAL: it does not re-litigate the shared points; it records that the named "sound" resource added nothing new and is a duplicate.

**Points** (all identical to sibling — see `signals-system-basic-drago.md` for full mapping)

| #   | Point                                                                                                      | Valid for our stack? | Already learned?       | Where / gap                                                                                                                                                                     | Verdict         |
| --- | ---------------------------------------------------------------------------------------------------------- | -------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 1   | Connect dynamic spawns by group sweep (`get_tree().get_nodes_in_group` + loop `.connect`).                 | holds with caveat    | partial                | Repo wires at spawn-time (wave_manager, enemy `died`); convention prefers parent-wires / `@export` over group sweep.                                                            | covered-ish     |
| 2   | Global autoload "signal hub" / event bus for cross-system events.                                          | **conflicts**        | covered (as forbidden) | godot-composition rule 5; godot-main-scene (autoload = data bag, no signals); godot-audio (NO AudioManager autoload — same stance for SFX); MEMORY "Health is a signal entity". | conflicts       |
| 3   | Safe disconnect: `if sig.is_connected(cb): sig.disconnect(cb)`.                                            | holds                | gap (minor)            | Correct Godot 4 API fact; not in any skill.                                                                                                                                     | gap (minor)     |
| 4   | Prefer disabling the seam (`set_deferred("disabled", true)`) over disconnect for temporary inactive state. | holds                | partial                | Matches calls-down / duck-typed-seam style.                                                                                                                                     | covered-ish     |
| 5   | SSOT: query real engine state (is-signal-connected) instead of a duplicate `is_ghost` bool that drifts.    | holds                | partial                | Spirit matches composition (no duplicate state); SSOT never named as a rule.                                                                                                    | gap (principle) |
| 6   | Unique node names (`%Name`) to shorten paths.                                                              | holds                | not in skills          | Editor convenience, orthogonal.                                                                                                                                                 | out of scope    |

**Conflict named** — Point 2 (event-bus autoload) contradicts **godot-composition rule 5** (no autoload/service-locator for cross-entity wiring), **godot-main-scene** (autoload = data bag, no signals/logic), **godot-audio** (no AudioManager autoload — relevant since filename implied sound), and **MEMORY: Health is a signal entity, not centralized**. Do NOT adopt the event-bus here; use parent-wires-at-spawn + `@export` injection + duck-typed seams.

**Recommended next** — nothing to dispatch. Duplicate of an already-harvested video; build is covered by existing godot-composition. The only additive bits (pts 3 + 5) are one-line notes for a builder to fold into `godot-composition` / `design/known-problems.md` IF a signals refactor touches them — not a new skill, no skill-researcher / addon-researcher / game-designer routing.

**Later** — (pt 3) `is_connected`-guard before `disconnect`; (pt 5) name SSOT as explicit anti-duplicate-state rule; (pt 4) prefer disable-seam over disconnect; (pt 6) `%`-unique-names. All minor; fold into composition convention on a future signals refactor. **Drop-zone hygiene**: this file was mislabeled `*-sound` — no audio content; the real audio source is still owed if SFX work is upcoming.
