# main.gd — starter shell. Replace this with your game.
# Levels load under the %LevelHost node; never change_scene_to_file()
# (see skill godot-main-scene). F5 boots to a lit, empty 3D space.
extends Node


func _ready() -> void:
	print("Xenodot starter running. Add a level under LevelHost to begin.")
